// I think the header needs to contain the sidebar, and the sidebar is only imported here rather than into App.js?

export default function Header () {
    return (
        <div><h1>Hello?</h1></div>
    )
}